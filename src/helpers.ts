const convertToBcd = (num: number, shift: number) => {};

const calcNumberLength = (num: number) => Math.floor(Math.log10(Math.abs(num)) + 1);

// const toBcd = (num) => {};

export { calcNumberLength };
